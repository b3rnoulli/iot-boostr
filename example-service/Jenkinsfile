pipeline {
    agent any
    tools {
        maven 'maven'
    }
    stages {
        stage('Build') {
            steps {
                sh 'cd example-service/'
                sh 'pwd'
                sh 'mvn clean package'
            }
        }
    }
}